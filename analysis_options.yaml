include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - _archive/**
    - _backup/**
    - backup_*/**
    - _hotfix_*/**
    - lib/**/_archive/**
    - lib/**/_backup/**
    - lib/**/backup_*/**
    - lib/**/_hotfix_*/**
    # ha bármi ideiglenes fájl van még:
    - "**/*.g.dart"
    - "build/**"
    - ".dart_tool/**"
